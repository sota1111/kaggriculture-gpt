# SOT-2779 — 需要時点premium saleと競合価格floorを移植する

- Classification: IMPLEMENT
- Decomposition: 不要
- Source: Seyamalam/Kaggriculture `8b8c421eb10634c756583ce10c75189f50c83a72`, `main.py`, MIT

## Result

The independent `DEMAND_TIMED_PREMIUM_SALES` candidate was evaluated against the SOT-2778 replay-identity corpus. It tied the champion on every screen and confirm rank/margin/tail metric, so the strict gate rejected it and the production `main.py` candidate was reverted. The checked-in measurement harness retains the distilled temporary candidate solely for reproducibility.

## Evidence

- Screen: mean rank `1.0`; mean/lower-tail/worst margin `1904/169/169` for both baseline and candidate.
- Confirm: mean rank `1.5`; mean/lower-tail/worst margin `-606.5/-1382/-1382` for both.
- Targeted both-seat firing: MILK `20→18`, WOOL `20→10`; matched-rival floor cap applied.
- `PROJECTED_MARKET_EXECUTION` stayed disabled with zero firings.
- 49 unit tests and submission contract/exec compatibility passed.
- Kaggle submission: not performed.

## Decision

Rejected; retain the current champion and preserve only measurement/test/ledger evidence.
