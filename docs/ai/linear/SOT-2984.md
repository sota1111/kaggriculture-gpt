# SOT-2984

- Title: Adaptive Replay oracleで独立whole-agent群をsealed比較する
- Status: In Progress → In Review
- Classification: IMPLEMENT
- Decomposition: 不要（上流3候補は SOT-2982 / SOT-2983 / SOT-2985 で完了済み）
- Branch: `feat/sot-2984-adaptive-replay-oracle`

## Acceptance

- [x] replay/source provenance と漏洩境界を manifest/report SHA-256 と local-only raw-byte boundary で固定
- [x] V111 / R5A / Conditional Memory / old champion の immutable artifact SHA-256 を固定
- [x] screen 未使用の2 lineages × 2 seeds × both seats で16 closed-loop matchesを実行
- [x] pessimistic p20/worst, rank stability, matchup spread を保存
- [x] experiment ledger に portfolio 結果を追記
- [x] Kaggle submission は未実行

## Verification

- `python3 -m compileall -q main.py scripts` — PASS
- `python3 -m unittest discover -s tests -v` — PASS (229 tests, 2 skipped)
- `bash scripts/build_submission.sh` + archive/contract check — PASS (generated archive reverted; production artifact unchanged)
- `git diff --check` — PASS

## Result

Conditional Memory was the strongest sealed hedge by the pre-registered ordering, but every policy lost both unseen matchups. It is therefore a non-representative-CV portfolio hedge only; the old champion remains retained and neither `main.py` nor `submission.tar.gz` is changed.
