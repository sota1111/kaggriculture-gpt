# SOT-2823

- Title: [kaggriculture-gpt] Kaggle順位向上サイクル第3次 — 改善方針の立案と実施
- Classification: PLAN / Kaggle improvement-cycle parent
- Decomposition: necessary; SOT-2824 → SOT-2826 → SOT-2825
- Resume branch: `feat/sot-2823-cycle-3-results`

## Child outcomes

- SOT-2824 (Done, PR #49): promoted a deterministic winner-only teacher dataset from six authenticated current-top public episodes. The 4,320-row dataset uses same-step public observations/actions only and holds team, episode, seed, and time identities apart between screen and confirm.
- SOT-2826 (Done, PR #50): distilled screen-only land/labor thresholds into a dependency-free compact policy. The component fired in direct A/B, but screen reward and margin regressed, so `COMPACT_REPLAY_POLICY` remained false.
- SOT-2825 (Done, PRs #51/#52): independently repeated the candidate on an untouched sealed cohort. Rank tied and relative margins improved, but candidate reward regressed by 1,194 in all four same-seed/both-seat matches; the strict non-regression gate rejected it and confirm was skipped.

## Parent decision

The external behavior-distillation workflow produced a reusable leak-free teacher foundation, but no runtime policy qualified for champion promotion. `COMPACT_REPLAY_POLICY=false` is retained, the executable artifact is unchanged, and no Kaggle submission is performed. This avoids spending the exhausted daily submission budget on a non-promoted candidate.

The next cycle should use first-action divergence analysis between the 4,320-row teacher dataset and champion traces to isolate one public-state decision family at a time. Land/labor threshold imitation is closed unless new evidence removes the repeated own-reward regression.

## Verification

- Python compile: PASS
- Unit tests: 84/84 PASS
- Fresh sealed-panel deterministic rerun: rejected; timing-excluded evidence reproduced
- Same-seed/both-seat intervention evidence: land 2 and labor 396 firings per match
- Submission contract: PASS
- gzip archive: single member containing only `main.py`
- npm lint/typecheck/e2e: N/A (Python-only repository; no package.json/browser surface)

