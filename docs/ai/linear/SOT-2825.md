# SOT-2825 — compact policy sealed closed-loop gate

The untouched `confirm` identities from the hash/license-pinned public closed-loop manifest were used as this issue's sealed screen because SOT-2826 fitted and screened only on the disjoint `screen` identities. Both seats and the same seed were evaluated directly for every identity.

The candidate preserved mean rank (2.0) and improved relative margins, but its own reward regressed by 1,194 in every match. The strict rank/reward/tail/runtime/contract gate therefore rejected the candidate. Independent confirm was skipped and `COMPACT_REPLAY_POLICY` remains disabled.

Evidence: `docs/measurements/SOT-2823/SOT-2825-compact-policy-sealed-gate.json`.
