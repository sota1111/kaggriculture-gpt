# SOT-2869 sealed V21 late-capital latch decision

The fresh screen used two public opponent archetypes, two new seeds, and both candidate seats. The
champion and candidate were run as a same-seed direct A/B. The candidate produced zero rank, margin,
lower-tail, worst, productive-completion, and terminal-cash delta, so the strict primary gate failed.
Targeted interventions independently confirmed both-seat firing, exact-once latching, suppression, and
metadata invariance. A second run reproduced the decision and every non-timing metric.

The untouched confirm identities were therefore not executed. The decision is `rejected`, not
`inconclusive`, because direct A/B and firing evidence are both present. The effective configuration is
`V21_ONE_TIME_LATE_CAPITAL_LATCH=false` (fingerprint
`586fbccc494dec840f7b2fb0e79f3cc0b9ef994a566a1952c8830dc5160777bb`); `main.py` and
`submission.tar.gz` were not changed or regenerated. Submission contract/exec compatibility passed,
and no Kaggle submission was performed.
