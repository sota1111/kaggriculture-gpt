# Multi-Route Farming Agent clean-room candidate

The referenced Kaggle notebook and output do not declare a redistribution
license, so neither is committed. `source.json` pins the notebook, output, and
archive hashes. `adapter.py` independently implements only the published
method: early public unlocked-shop order selects a yarn-led, milk-supported,
or balanced complete route foundation.

The adapter builds offline with the exact MIT-licensed lonespear foundation
into one standard-library-only `main.py`. It is default-OFF; the champion and
`submission.tar.gz` remain unchanged.

Run `python scripts/measure_multi_route_farming_agent.py` to reproduce the
same-seed/both-seat screen and the identity-disjoint sealed confirm.
