# Licensed whole-agent hedge

This directory vendors the exact MIT-licensed submission artifact from
`deepeshumrao/kaggriculture-agent@65724ce530af8e8ea0410d5b7f0e2a997ca676cb`.
It is an independent, default-OFF hedge and does not change `main.py`.

Provenance and hashes are in `source.json`; copyright, attribution, and the
license are retained in `NOTICE.md` and `LICENSE-MIT.txt`. Reproduce the
both-seat screen and disjoint confirm with:

```bash
python3 scripts/measure_deepeshumrao_whole_agent.py
```

The committed measurement is evidence only. It performs no Kaggle submission
and does not promote the candidate based on public score.
