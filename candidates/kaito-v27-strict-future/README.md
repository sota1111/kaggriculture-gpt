# Kaito v27 Strict-Future whole-agent candidate

This directory intentionally contains metadata only. The exact public output is
hash-pinned and evaluated transiently, but is not redistributed because neither
the downloaded Kaggle metadata nor the notebook body declares a license.

`scripts/measure_kaito_v27_strict_future.py` fetches (or accepts through
`KAITO_V27_MAIN_PATH`) the exact 20,813-byte artifact, verifies its identity,
standard-library boundary and route contract, and then runs the preregistered
screen and chronological sealed-confirm panels. `main.py` remains the unchanged
incumbent hedge, and this candidate is never enabled or submitted.
