# Strict-Future meta-reset candidate

This is an independent, default-OFF whole-agent candidate. The builder combines
the unchanged repository champion with `policy.py`; it never modifies `main.py`
or `submission.tar.gz`.

The referenced public notebook did not declare a license in its downloaded
metadata or prose. Its embedded 21,330-byte artifact is therefore fetch-only
evidence and is not copied or redistributed here. `policy.py` is a clean-room
implementation of two public descriptions: redirecting existing opening animal
orders into a sheep-first basin and
reordering only already-existing SELL slots from current public prices,
inventory, and town demand. It contains no published route, replay actions,
identity fields, future state, or weights.

Build an enabled evaluation artifact with:

```bash
python scripts/package_strict_future_meta_reset.py --enable --output /tmp/strict-future.py
```

Confirm identities remain sealed for the parent portfolio gate; this child
opens only the chronological screen panel.
