# Third-party attribution

`agent.py` is the exact output from Kaito Fukami's Kaggle notebook
“41/49 Future+Unseen | v19 Replication to Control”, script version
`340133955`. Kaggle marks that version as Apache-2.0.

The exact upstream notebook, output `main.py`, output archive, and packaged
agent hashes are pinned in `source.json`. The source is vendored without policy
changes; notebook logs, replay/private traces, credentials, and weights are not
included.
