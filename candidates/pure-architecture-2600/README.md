# Pure Architecture 2600 provenance boundary

This directory intentionally contains no source agent. The public Kaggle notebook does not declare a
redistribution license, so its embedded payload is decoded only in a temporary directory by the
measurement script after the notebook and agent SHA-256 values match `source.json`.

The public “2600+ Elo” wording is not promotion evidence. The independent same-seed, both-seat screen
and confirm panels are the only performance evidence. `main.py` and `submission.tar.gz` remain the
unchanged champion/hedge.
